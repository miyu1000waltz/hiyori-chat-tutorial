using UnityEngine;

// ダミーファイル。Part3にて公開し・使用予定。

public class AsrApiController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
